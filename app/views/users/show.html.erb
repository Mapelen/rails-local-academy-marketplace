<div class="wrapper style1">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-10 col-sm-offset-1">
        <div class="flex-box">
          <div class="img-box">
            <%= image_tag current_user.photo.url, class: "avatar-xxl" %>
          </div>
          <div class="headbox text-center">
            <h2><%= current_user.first_name %> <%= current_user.last_name %></h2>
            <h4 class="bio">" <%= current_user.bio %> "</h4>
            <ul class="list-inline list-unstyled">
              <li>Mail: <%= current_user.email %></li>
              <li>Company: <%= current_user.company %></li>
            </ul>
            <% if current_user == @user %>
              <div class="text-center"><%= link_to "Edit profile", edit_user_path(current_user), class: "btn btn-primary" %></div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<!--
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1 text-center">
    </div>
  </div>
</div> -->
